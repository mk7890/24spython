import random # import the random number library
start_number = 1 # assign the starting range number as 1
last_number = 100 # assign the last number in the range as 100
random_number = random.randint(start_number, last_number) #generate a random number between 1 and 100 and assign it to random_number variable
print(random_number) # print the random_number on the terminal